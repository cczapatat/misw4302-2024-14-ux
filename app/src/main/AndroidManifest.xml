<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.uniandes.edu.uxalarm">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon_name"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_name_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UxAlarm"
        tools:targetApi="31">
        <activity
            android:name=".CreateAlarmActivity"
            android:exported="false" />
        <activity
            android:name=".ScanQRActivity"
            android:exported="false" />
        <activity
            android:name=".ScanQr"
            android:exported="false" />
        <activity
            android:name=".AlarmsActivity"
            android:exported="false" />
        <activity
            android:name=".LoaderActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.UxAlarm">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>